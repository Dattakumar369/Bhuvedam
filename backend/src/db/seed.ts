/**
 * Seed Bhuvedam crop catalog (Telugu + English details) into Neon.
 * Run: npm run db:seed:crops
 * After editing constants/crops.ts: npm run export:crops (from backend folder)
 */
console.error('Use "npm run db:seed:crops" to seed the Bhuvedam crop catalog into Neon PostgreSQL.');
process.exit(1);
